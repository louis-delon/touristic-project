<h1>Adresses à visiter Absolument</h1>

<%= link_to "retour", admins_root_path %>


<% @addresses.each do |address| %>
  <div><%= address.site.title %></div>
  <div><%= address.site.opening_time %></div>
  <div><%= address.site.description %></div>
  <div><%= address.site.phone_number %></div>
  <div><%= address.site.website %></div>
  <div><%= address.site.category %></div>
  <div><%= image_tag(address.site.picture, class: "image") %></div>
  <div><%= link_to "Voir", admins_sector_site_path(address.sector, address.site) %> </div>
  <div><%= link_to "Editer", edit_admins_sector_site_path(address.sector, address.site) %> </div>
  <div><%= link_to "Supprimer", admins_sector_site_path(address.sector, address.site), method: :delete %> </div>
<% end %>
