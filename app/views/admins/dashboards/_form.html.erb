<%= form_with model: [:admins, @sector] do |f| %>
  <div data-controller="sectors">

    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, data: { target: "sectors.name" } %>
    <div >

    <div class="form-group">
      <%= f.label :description %>
      <%= f.rich_text_area :description, placeholder: '400 caractères max' %>
    </div>

    <div  class="form-group">
      <%= f.label :city%>
      <%= f.text_field :city, label: 'Ville', data: { target: "sectors.city" } %>
    </div>

    <div class="form-group">
      <%= f.label :illustration%>
      <%= f.file_field :illustration%>
    </div>

     <div data-action="click->sectors#insert"><%= f.button :submit %></div>

  </div>
<% end %>
